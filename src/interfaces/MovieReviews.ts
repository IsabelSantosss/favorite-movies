export interface MovieReviews {
      author: string;
      authorDetails: { name: string, username: string, avatarPath: string, rating: 6 };
      content: string;
      createdAt: string;
      updatedAt: string;
      url: string;
}  